---
'@finos/legend-application-marketplace': patch
---

Render the Agents tab answers with the shared `LegendAIAssistantMessageView` instead of a forked copy, and clamp the agent input textarea height so long questions no longer grow it without bound.
