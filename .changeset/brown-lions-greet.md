---
'@finos/legend-lego': minor
---

Extract the Legend AI assistant message renderer into a shared `LegendAIAssistantMessageView` component so every surface renders the same answer, thinking-step and artifact UI.
